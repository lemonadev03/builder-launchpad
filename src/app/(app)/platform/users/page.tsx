import { PlatformSectionPlaceholder } from "@/components/platform-section-placeholder";

export default function PlatformUsersPage() {
  return (
    <PlatformSectionPlaceholder
      eyebrow="Phase 15.3"
      title="User management"
      description="Platform-level user search, suspension, and deletion tools will build on this route."
      nextTicket="15.3 User Management (Platform)"
    />
  );
}
