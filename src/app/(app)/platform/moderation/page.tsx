import { PlatformSectionPlaceholder } from "@/components/platform-section-placeholder";

export default function PlatformModerationPage() {
  return (
    <PlatformSectionPlaceholder
      eyebrow="Phase 15.4"
      title="Platform moderation"
      description="The global moderation queue will connect the existing flag system to platform-admin workflows here."
      nextTicket="15.4 Platform Moderation"
    />
  );
}
