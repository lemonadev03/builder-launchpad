import { PlatformSectionPlaceholder } from "@/components/platform-section-placeholder";

export default function PlatformTagsPage() {
  return (
    <PlatformSectionPlaceholder
      eyebrow="Phase 15.5"
      title="Tag management"
      description="The profile tag catalog will be managed here once the platform settings ticket lands."
      nextTicket="15.5 Platform Settings"
    />
  );
}
